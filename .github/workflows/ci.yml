name: NX°

on:
  pull_request:
    branches:
      - staging
      - master
  push:
    branches:
      - staging
  workflow_dispatch:

# Workflow semantic version (manual for now):
# - bump PATCH for text/placeholder-only edits (v0.1.1)
# - bump MINOR for non-breaking workflow behavior updates (v0.2.0)
# - bump MAJOR for breaking CI flow changes (v1.0.0)
env:
  WORKFLOW_VERSION: 0.1.0

jobs:
  placeholder:
    name: Placeholder only (no real CI)
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Placeholder
        run: |
          echo "CI workflow is currently a placeholder."
          echo "Workflow version: ${WORKFLOW_VERSION}"

      # Real CI is intentionally disabled for now.
      # Uncomment and restore when ready to re-enable pipeline execution.
      # - name: Checkout
      #   uses: actions/checkout@v4
      #
      # - name: Setup Node.js
      #   uses: actions/setup-node@v4
      #   with:
      #     node-version: 20
      #     cache: yarn
      #
      # - name: Install dependencies
      #   run: yarn install --frozen-lockfile
      #
      # - name: Lint
      #   run: yarn lint
      #
      # - name: Build
      #   run: yarn build
